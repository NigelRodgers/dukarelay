# Normalise line endings to LF in the repo (matches .editorconfig / WPCS style),
# regardless of the contributor's OS.
* text=auto eol=lf

# Binary types Git must never touch.
*.png binary
*.jpg binary
*.zip binary
